# File Integrity Checker (Hashing Utility) 🔐

โปรเจกต์ที่เน้นด้าน **Digital Forensics** และ **Data Integrity** โดยใช้ Python ในการคำนวณและเปรียบเทียบ Cryptographic Hash ของไฟล์ เพื่อตรวจสอบว่าไฟล์นั้นถูกดัดแปลงหรือไม่

---

## 📝 จุดประสงค์และหลักการทำงาน

### จุดประสงค์
1.  เพื่อฝึกการใช้งานไลบรารี **`hashlib`** ของ Python ซึ่งเป็นพื้นฐานสำคัญของ Cryptography
2.  เพื่อแสดงความเข้าใจในแนวคิดของ **Data Integrity** และวิธีการยืนยันความสมบูรณ์ของหลักฐานดิจิทัล (Digital Evidence)

### หลักการทำงาน
1.  ผู้ใช้เลือกโหมดการทำงาน (คำนวณ Hash หรือ เปรียบเทียบไฟล์) และเลือกอัลกอริทึม (MD5, SHA256)
2.  สคริปต์จะอ่านไฟล์ทีละส่วน (**chunking**) เพื่อให้สามารถจัดการกับไฟล์ขนาดใหญ่ได้อย่างมีประสิทธิภาพ
3.  ใช้ฟังก์ชัน Hash (เช่น SHA-256) เพื่อสร้าง "ลายนิ้วมือดิจิทัล" ที่ไม่ซ้ำกันของไฟล์
4.  หากไฟล์สองไฟล์มีค่า Hash ตรงกัน แสดงว่าไฟล์เหล่านั้น **เหมือนกันทุกประการ** หากค่า Hash ต่างกันแม้แต่นิดเดียว แสดงว่าไฟล์ถูก **ดัดแปลง (Tampered)**

## 🚀 การติดตั้งและใช้งาน

### ข้อกำหนดเบื้องต้น

* **Python 3.x**
* ไม่จำเป็นต้องติดตั้งไลบรารีภายนอก

### ขั้นตอนการใช้งาน

รันสคริปต์ผ่าน Command Line:

```bash
python file_hasher.py
